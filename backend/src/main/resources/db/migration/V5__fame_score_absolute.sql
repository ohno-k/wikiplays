-- ============================================================================
--  V5: 知名度スコアを絶対基準 (1 日あたり閲覧数の log10) に変更
--    V4 のスコアは (他言語版数・記事長・別名数) の相対的な重み付き和で、
--    (scope, genre) バケット内の順位でしか意味を持たなかった。
--    新しいスコアは Wikipedia の閲覧数を基準にした絶対値で、tier の境界は固定値になる。
--    単位が変わるので全件を未計算 (NULL) に戻し、起動時のバックフィルで再計算させる。
--    (閲覧数そのものは data_json 内に保存され、未取得の記事は起動時に順次取得される)
-- ============================================================================

UPDATE cached_article SET fame_score = NULL;
